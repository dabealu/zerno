# Installation

Install dependancies:
```sh
# node needed by bash and yaml language servers, node depends on simdjson
# font with icons - set it in terminal config
pacman -Sy neovim ripgrep fd fzf lazygit nodejs simdjson ttf-jetbrains-mono-nerd
```

Copy configs:
```sh
cp -r nvim ~/.config/nvim
```

