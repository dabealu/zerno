# soften the intel split-lock penalty: since kernel 5.19 the kernel stalls
# processes taking split locks, which cripples some proton/wine games.
# detection warnings are kept, only the stall is removed.
# deployed only when /proc/cpuinfo reports GenuineIntel.
kernel.split_lock_mitigate = 0
